# Internship report

`BIO395_FinalReport_DRAFT.docx` is generated by `build_report.py` from the pipeline's own output, so every
number in it comes from `results/summary.json` and the tables underneath it. Regenerate after any re-run:

```bash
python3 report/build_report.py --results results --out report/BIO395_FinalReport_DRAFT.docx
```

## Before submitting

1. Fill the **23 red `[TO COMPLETE]` placeholders** — company facts, supervisor, dates, and the personal
   sections (5.1 learning, 5.4 a typical day) that only you can write.
2. Insert the automatic table of contents in Word (References → Table of Contents → Automatic Table). The
   headings already use the built-in Heading 1/2/3 styles.
3. Rename to the required convention:
   `BIO395_FinalReport_FIRSTNAME_LASTNAME_DDMonthYYYY.doc`
4. Check the length against the section limits: company information ≤3 pages, each of 3.1–3.3 ≤1 page,
   related literature ≤3 pages, 4.5 details ≤10 pages, 4.6 results ≤1 page, section 5 ≤3 pages,
   conclusions and recommendations ≤1 page each.
5. Confirm the file is under 10 MB and that no patient identifier appears anywhere. The specimen is referred
   to by an alias throughout and the laboratory's sample code is not used.

## Figures worth adding

The report is currently text and tables only. Four figures would strengthen it, and the data for each is
already produced by the pipeline:

| Figure | Source |
|---|---|
| Per-position base composition of Read 1, showing the barcode and the fixed adapter region | `results/qc/read_structure_DNA.json` → `positional_consensus` |
| Coverage distribution across panel targets | `results/galaxy_import/coverage.regions.bed` |
| Caller agreement (how many variants each caller found, and the overlap) | `results/dna/variants_merged_summary.json` |
| Substitution spectrum of somatic candidates, showing the dominant class | `results/dna/biomarkers.json` → `mutation_spectrum` |

Screenshots of the dashboard pages also work well in section 4.5.
